<nav class="bg-gray-800">
    <div class="max-w-full px-2 mx-auto sm:px-4 lg:px-6">
        <div class="relative flex items-center justify-between h-12">
            <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
                <!-- Mobile menu button-->
                <HambergerButton IsNavMenuOpen="@(!isNavMenuOpen)" ToggleNavMenu="@ToggleNavMenu" />
            </div>

            <div class="flex items-center justify-center flex-1 sm:items-stretch sm:justify-start">
                <!-- Logo -->
                <div class="flex items-center flex-shrink-0">
                    <h3 class="text-gray-200 ">My Logo</h3>
                </div>
                <!-- Main Menu -->
                @* <div class="hidden sm:block sm:ml-6">
                    <div class="flex space-x-4">
                    <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
                    <NavLink href="/" ActiveClass="active-btn" class="nav-btn" aria-current="page"
                    Match=NavLinkMatch.All>
                    Home</NavLink>
                    <NavLink href="/counter" ActiveClass="active-btn" class="nav-btn">Counter</NavLink>
                    </div>
                    </div>
                *@
            </div>
            <!-- Right Icon -->
            <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
                <div class="hidden sm:block sm:ml-6">
                    <div class="flex space-x-4">
                        <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
                        <NavLink href="/" ActiveClass="active-btn" class="nav-btn" aria-current="page"
                            Match=NavLinkMatch.All>
                            Home</NavLink>
                        <NavLink href="/counter" ActiveClass="active-btn" class="nav-btn">Counter</NavLink>
                    </div>
                </div>
                <BellButton />
            </div>
        </div>
    </div>

    <!-- Mobile menu, show/hide based on menu state. -->
    <div class="@showMobileMenu sm:hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1">
            <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
            <NavLink href="/" ActiveClass="active-btn" class="nav-btn mobile-btn" Match=NavLinkMatch.All>Home</NavLink>
            <NavLink href="/counter" ActiveClass="active-btn" class="nav-btn mobile-btn">Counter</NavLink>
        </div>
    </div>
</nav>

@code {
    bool isNavMenuOpen { get; set; } = true;
    string navMenuClass => isNavMenuOpen ? "" : "hidden";
    string showMobileMenu => !isNavMenuOpen ? "" : "hidden";
    void ToggleNavMenu()
    {
        isNavMenuOpen = !isNavMenuOpen;
    }

    string hidePprofileMenu => isProfileMenuOpen ? "" : "hidden";
    bool isProfileMenuOpen { get; set; } = false;
    void OnProfileBtnClick()
    {
        isProfileMenuOpen = !isProfileMenuOpen;
    }
    void OnProfileBtnBlur()
    {
        //isProfileMenuOpen = false;
    }
}